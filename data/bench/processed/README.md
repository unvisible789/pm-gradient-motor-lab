# Processed Bench Data

Place cleaned bench-run CSV files here after raw DAQ export normalization.

Required columns for `validation.audit_bench_run`:

- `time_s`
- `voltage_v`
- `current_a`
- `torque_nm`
- `rpm`
